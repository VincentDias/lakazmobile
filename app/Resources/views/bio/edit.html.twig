{% extends 'base.html.twig' %}
{% block body %}
    <div class="row">
        <div class="col-12 col-sm-12 col-md-12 col-lg-12 text-center">
            <h1>Modification de la bio</h1>
        </div>
    </div>

    <div class="row d-flex justify-content-center form-group">
        <div class="col-10 col-sm-10 col-md-8 col-lg-8 text-center">
            {{ form_start(edit_form, {'attr':{'novalidate': 'novalidate'}}) }}

            {{ form_label(edit_form.title, 'Titre')}}
            {{ form_widget(edit_form.title, {'attr':{'class':'form-control'}})}}
            {{ form_errors(edit_form.title)}}
        
            {{ form_label(edit_form.description, 'Contenu')}}
            {{ form_widget(edit_form.description, {'attr':{'class':'form-control'}})}}
            {{ form_errors(edit_form.description)}}
        </div>
    </div>

    <div class="row">
        <div class="col-12 col-sm-12 col-md-12 col-lg-12 text-center">
            {{ form_widget(edit_form) }}
            <input class="btn btn-warning" type="submit" value="Enregistrer"/>
            {{ form_end(edit_form) }}
        </div>
    </div>

    <div class="row">
        <div class="col-12 col-sm-12 col-md-12 col-lg-12 text-center">
            <button class="btn btn-warning" type="button">
                <a href="{{ path('article_index') }}">Retour</a>
            </button>
        </div>
    </div>
{% endblock %}