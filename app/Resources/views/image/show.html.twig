{% extends 'base.html.twig' %}

{% block body %}
    <h1>Image</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ image.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ image.name }}</td>
            </tr>
            <tr>
                <th>Balise "Alt"</th>
                <td>{{ image.alt }}</td>
            </tr>
            <tr>
                <th>Url de l'image</th>
                <td>{{ image.pathImage }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('image_index') }}">Revenir à la liste des images</a>
        </li>
        <li>
            <a href="{{ path('image_edit', { 'id': image.id }) }}">Editer</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
